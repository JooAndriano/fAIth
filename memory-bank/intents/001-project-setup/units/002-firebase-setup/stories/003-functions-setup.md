# Story 003 — Functions Setup

**Intent:** 001-project-setup
**Unit:** 002-firebase-setup
**Story:** 003-functions-setup
**Status:** Draft

## User Story
As a developer, I want Firebase Functions scaffolded so we can later add the Gemini proxy securely.

## Acceptance Criteria
- `firebase init functions` completed (TypeScript).
- Local build works.
- `firebase deploy --only functions` succeeds.
- Placeholder endpoint returns static response.
